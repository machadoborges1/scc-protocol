# Use the official Matchstick image with a specific version for reproducibility.
FROM limechain/matchstick:v0.5.5

# Set the working directory inside the container.
WORKDIR /matchstick

# The project files will be mounted here at runtime via the docker run command.
# The entrypoint is already configured in the base image to run Matchstick.
