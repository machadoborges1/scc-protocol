{
  "name": "@scc/subgraph",
  "version": "1.0.0",
  "private": true,
  "description": "GraphQL indexer for the SCC protocol.",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy:local": "graph deploy --node http://127.0.0.1:8020/ --ipfs http://127.0.0.1:5001 scc/scc-protocol",
    "create:local": "graph create --node http://127.0.0.1:8020/ scc/scc-protocol",
    "remove:local": "graph remove --node http://127.0.0.1:8020/ scc/scc-protocol",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.78.0",
    "@graphprotocol/graph-ts": "^0.34.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
